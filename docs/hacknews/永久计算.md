# 永久计算 / Permacomputing

URL: https://wiki.xxiivv.com/site/permacomputing.html

## A holistic approach to computing inspired from permaculture.
## 一种受朴门永续设计启发、关于计算的整体性方法。

Permacomputing is a design practice that encourages the maximization of hardware lifespan, minimization of energy usage and focuses on the use of already available computational resources. It values maintenance and refactoring of systems to keep them efficient, instead of planned obsolescence, permacomputing practices planned longevity. It is about using computation only when it has a strengthening effect on ecosystems.

永久计算是一种设计实践，它鼓励最大化硬件寿命、最小化能源消耗，并专注于使用已有的计算资源。它重视对系统的维护和重构以保持其高效，而非计划性淘汰；永久计算实践的是计划性长寿。它是关于仅在计算对生态系统具有强化作用时才使用计算。

---

> Articulating the Value of Absence
> 阐述缺席的价值

**Frugal Computing** / **节俭计算**

Utilizing computational resources as finite, to be utilized only when necessary, and as effectively as possible.

将计算资源视为有限的，仅在必要时使用，并尽可能有效地利用。

**Salvage Computing** / **抢救计算**

Utilizing only already available computational resources, to be limited by that which is already produced.

仅利用已有的计算资源，受限于已生产出的资源。

**Collapse Computing** / **崩溃计算**

Utilizing what has survived the collapse of industrial production or network infrastructure.

利用那些在工业生产或网络基础设施崩溃后幸存下来的东西。

---

Designing for Disassembly ensures that all elements of a product can be disassembled for repair and for end of life. This allows for and encourages repairs, with the result that a product's life cycle is prolonged; and it allows for a product to be taken apart at the end of its life so that each component can be reclaimed.

为拆卸而设计确保产品的所有元素都可以被拆卸以进行维修和报废处理。这允许并鼓励维修，从而延长产品的生命周期；它还允许产品在生命周期结束时被拆解，以便每个组件都可以被回收。

That which cannot be repaired is already broken.

无法维修的东西已经坏了。

This means using simple mechanical fasteners instead of adhesives, clearly labeling components with their material type, and ensuring components can be disassembled with everyday tools. Unlike the nebulous goal of designing a sustainable product, designing a product for disassembly is a more concrete, quantifiable approach to ecologically sound making and to consumption.

这意味着使用简单的机械紧固件而非粘合剂，用材料类型清晰标注组件，并确保组件可以用日常工具拆卸。与设计可持续产品这一模糊目标不同，为拆卸设计是一种更具体、可量化的生态友好制造和消费方法。

> — Pictographs on Voyager's Golden Record cover.
> — 旅行者号金唱片封面上的象形图。

A self-obviating system is a system which renders itself unnecessary in the long term. It is in opposition to systems designed to maximise user engagement. It tries to make itself less and less necessary to the realization of its purpose, and gradually allow people to provide for their own welfare.

自淘汰系统是一种从长远来看使自身变得不必要的系统。它与旨在最大化用户参与度的系统相对立。它试图使自己对实现其目标的必要性越来越小，并逐渐让人们能够自给自足。

---

- [Terminology And Practices](https://computingwithinlimits.org/2021/papers/limits21-devalk.pdf)
- [术语与实践](https://computingwithinlimits.org/2021/papers/limits21-devalk.pdf)

- [When the Implication Is Not to Design](https://www.ics.uci.edu/~djp3/classes/2011_01_INF134/papers/impl9-rev.pdf)
- [当暗示是不设计时](https://www.ics.uci.edu/~djp3/classes/2011_01_INF134/papers/impl9-rev.pdf)

- [Permacomputing Wiki](https://permacomputing.net)
- [永久计算维基](https://permacomputing.net)

- [Frugal Computing](https://wimvanderbauwhede.github.io/articles/frugal-computing/)
- [节俭计算](https://wimvanderbauwhede.github.io/articles/frugal-computing/)

---

## Open computation that goes beyond open source, made from parts designed to be recombined.
## 超越开源的开放计算，由设计为可重组的部件组成。

A malleable system is designed to support arbitrary recombination, reused across environments and freely sharable with others. Modifying such a system happens in the context of use, rather than through some separate tool and skill set.

可延展系统设计用于支持任意重组，可跨环境重复使用，并可自由与他人共享。修改此类系统发生在使用情境中，而非通过某些单独的工具和技能。

In contrast with monolithic applications, these amorphous computers are finely tuned and thoughtfully crafted by their operator over the course of a practice.

与单体应用相比，这些无定形的计算机由操作者在其实践过程中精心调整和打造。

At its core, a malleable system is made of heterogeneous parts capable of assemblage. Within the context of computation, it is an attribute shared by different interoperable systems, not unlike programs that communicate via unix pipes, file browsers and some flow-based programming environments.

可延展系统的核心是由能够组合的异质部件组成。在计算情境中，它是不同可互操作系统共享的属性，类似于通过 Unix 管道通信的程序、文件浏览器和一些基于流的编程环境。

A malleable part is a transformer in a chain of transformers, and is itself also made of equally interconnected links, nodes or agents. The more malleable a system, the blurrier the distinction in the granularity of these parts.

可延展部件是变换器链中的一个变换器，其本身也由同样相互连接的链接、节点或代理组成。系统越可延展，这些部件的粒度区分就越模糊。

---

> Malleability
> 可延展性

**Silo** / **孤岛**

A system in which nodes will forcefully captures the message is a silo.

节点强制捕获消息的系统即为孤岛。

**Shallow** / **浅层**

Messages can enter and leave a system that is not malleable itself, this is malleability of a superficial kind.

消息可以进入和离开一个本身不可延展的系统，这是一种表面的可延展性。

**Average** / **平均**

Messages can flow freely between internal nodes of a malleable system, but capabilities are restricted by domain, interface, or operator competency.

消息可以在可延展系统的内部节点之间自由流动，但能力受限于领域、界面或操作者能力。

**Deep** / **深层**

Messages can be passed seamlessly across every level of the system, the division into layers has entirely vanished.

消息可以无缝传递到系统的每个层级，层级划分已完全消失。

---

A potential advantage to this granularity is the distribution of complexity so that an operator can more gently transition toward a deeper understanding of the finer parts that makes up the solution to a problem.

这种粒度的一个潜在优势是复杂性的分布，使操作者能够更温和地过渡到了解构成问题解决方案的更精细部分。

Designing for reversibility is to make a system approachable to a wide range of competency, lower the cost of errors, and limit the blame put upon its operator, by favoring transformations that can be undone.

为可逆性而设计是使系统能够被各种能力水平的人接近，降低错误成本，并通过支持可撤销的转换来限制对操作者的指责。

Some automata conserve enough state through their transformation that computation can happen backward, without cluttering large amount of memory with screenshots.

一些自动机在其转换中保存了足够的状态，使计算可以反向进行，而无需用大量内存来存储截图。

Familiar examples of reversible computation are the multiplication of a number by a fraction and using the reciprocal to recover that number, or the stack machine operation rot. The bitwise operator not or the stack machine swap are involutive, which means that same operator can applied once again to recover the earlier state.

可逆计算的常见例子是数字乘以分数并使用倒数恢复该数字，或堆栈机操作 rot。位运算符 not 或堆栈机 swap 是对合的，这意味着再次应用相同的运算符可以恢复先前的状态。

---

- [Inverse Operators](https://vine.dev/docs/features/inverse), Vine Language
- [逆运算符](https://vine.dev/docs/features/inverse)，Vine 语言

- [Thermodynamics of Garbage Collection](../docs/baker_thermodynamics.html), Henry G. Baker
- [垃圾收集的热力学](../docs/baker_thermodynamics.html)，Henry G. Baker

- [Undoing Register Machines](fractran.html#reversibility), Fractran
- [撤销寄存器机](fractran.html#reversibility)，Fractran

---

Designing for concatenation is using a point-free message passing paradigm that does not identify the arguments on which it operates. It is mentioned here as it might prove valuable in a Deeply Malleable system as a way to combined both inspectability down to its the mechanical transformation, and reasonable evaluation performance.

为连接而设计是使用一种无点的消息传递范式，不识别其操作的参数。此处提及它是因为它可能在深度可延展系统中被证明是有价值的，作为结合可检查性到机械转换层面和合理评估性能的一种方式。

```
: SQUARED ( n -- n^2 ) DUP * ;
: CUBED ( n -- n^3 ) DUP SQUARED * ;
```

In such a system, nodes merely compose other nodes, among which are combinators that manipulate the arguments, regardless of context. This is a fancy way of saying that a concatenative sub-expression can be replaced with a name that represents that same sub-expression.

在这样的系统中，节点仅组合其他节点，其中包括组合子，它们操作参数而不考虑上下文。这是一种花哨的说法，即连接性子表达式可以用代表该相同子表达式的名称替换。

```
: POPCNT ( n -- bits ) 0 swap
BEGIN dup WHILE tuck 1 AND + swap 1 rshift REPEAT
DROP ;
```

A maximally accessible system such that the finer details in movement of bits are observable but that does not run on generic hardware would be paradoxical by virtue of its inaccessibility. For that reason, depending on the host computer, a function definition like the one above(which might exist as a single opcode), should probably still be part of the observable system definitions even if it is not evaluated.

一个最大可访问的系统，使得位移动的更精细细节是可观察的，但不在通用硬件上运行，这将因其不可访问性而显得自相矛盾。因此，根据宿主计算机的不同，像上面的函数定义（可能作为单个操作码存在）应该仍然是可观察系统定义的一部分，即使它不被执行。

Designing for differences is to consider the compatibility between wildly different interfaces and notations to support networks of people collaborating, across many levels of competency, to make software work for themselves and their communities.

为差异而设计是考虑 wildly different 界面和表示法之间的兼容性，以支持跨越许多能力水平的人们协作，使软件为他们自己和他们的社区工作。

The rewriting language Maude, uses a mixfix notation, making the system neither prefix, infix, or postfix, but adaptable to the user's needs and background. To demonstrate something akin to the mixfix notation and give a taste of rewriting, consider a system made of rules where a given left-hand side is replaced by its corresponding right-hand side, and where ?words will match anything.

重写语言 Maude 使用混合中缀表示法，使系统既不是前缀、中缀或后缀，而是适应用户的需求和背景。为了演示类似混合中缀表示法的内容并给出一丝重写的感觉，考虑一个由规则组成的系统，其中给定的左侧被替换为相应的右侧，其中 ?words 将匹配任何内容。

```
(add ?x ?y) -> (?x + ?y)
(?x ?y add) -> (?x + ?y)

((?a) + ?b) -> (?a + (?b))
(0 + ?b) -> (sum ?b)

(add 1 2)
```

The first two rules of the program above both enable the translation between prefix & postfix, to the infix notation, as well as transcribing the english symbol add that could otherwise be localized. This is to demonstrate that inter op between vastly different systems is possible.

上面程序的前两条规则都支持前缀和后缀到中级表示法的翻译，以及转录英语符号 add，否则可以进行本地化。这是为了演示 vastly different 系统之间的互操作是可能的。

---

- [Hedy](https://hedy.org), multi-lingual PL.
- [Hedy](https://hedy.org)，多语言编程语言。

- [No Stinking Loops](https://nsl.com), array notation.
- [No Stinking Loops](https://nsl.com)，数组表示法。

---

> Asking for the most suitable programming language for permacomputing is akin to asking for the most suitable plant for permaculture — the entire question contradicts itself.
> 询问最适合永久计算的编程语言，就像询问最适合朴门永续设计的植物——整个问题本身就是自相矛盾的。

> — Permacomputing Wiki, Programming Languages
> — 永久计算维基，编程语言

---

Designing for bootstrapping is to make a system which does not depend on a binary seed that cannot be built from source. The goal is to start from a minimal, easily inspectable binary which should be readable as source and bootstrap into a practical and reproducible user interface.

为引导而设计是使一个不依赖于无法从源代码构建的二进制种子的系统。目标是从一个最小、易于检查的二进制文件开始，该文件应可作为源代码阅读，并引导进入实用且可重现的用户界面。

A build is reproducible if given the same source code, build environment and build instructions, any party can recreate bit-by-bit identical copies of all specified artifacts. To accomplish this:

如果给定相同的源代码、构建环境和构建指令，任何一方都可以重新创建所有指定产物的逐位相同副本，则该构建是可重现的。为实现这一点：

- The build system needs to be made entirely deterministic. Transforming a given source must always create the same result. For example, the current date and time must not be recorded and output always has to be written in the same order.

- 构建系统需要完全确定。转换给定源代码必须始终创建相同结果。例如，当前日期和时间不得被记录，输出必须始终以相同顺序写入。

- The set of tools used to perform the build and more generally the build environment should either be recorded or pre-defined.

- 用于执行构建的工具集，以及更一般的构建环境，应该被记录或预定义。

- Users should be given a way to recreate a close enough build environment, perform the build process, and validate that the output matches the original build.

- 用户应该有一种方法来重新创建足够接近的构建环境，执行构建过程，并验证输出与原始构建匹配。

Deployment (n.): getting computer programs from one machine to another and having them still work when they get there.

部署（名词）：将计算机程序从一台机器移动到另一台机器，并在到达后仍能使它们工作。

> The Canon Cat is written in Forth, a concatenative programming environment available to the operator as a subsystem scripting language.
> 佳能猫用 Forth 编写，这是一种可供操作者作为子系统脚本语言使用的连接式编程环境。

---

- [Malleable Essay](https://www.inkandswitch.com/essay/malleable-software), Ink & Switch
- [可延展文章](https://www.inkandswitch.com/essay/malleable-software)，Ink & Switch

- [Malleable Systems](https://malleable.systems), forum
- [可延展系统](https://malleable.systems)，论坛

---

## Figuring out how to make the best possible use out of the millions of devices which already exist.
## 弄清楚如何最好地利用已经存在的数百万台设备。

Salvage computing believes that the end of a computer product's lifecycle should be seen as a moment of celebration, a moment when its socioeconomic context can finally be reclaimed. Scavenge-friendly electronics are parts that are no longer manufactured, but that are available by the billions in landfills. Those who can manage to create new designs from scavenged parts with low-tech tools will be able to preserve electronics.

抢救计算认为，计算机产品生命周期的终点应被视为庆祝的时刻，是其社会经济背景终于可以被回收的时刻。可拾荒电子零件是那些不再生产，但在垃圾填埋场中以数十亿计的可用零件。那些能够利用低技术工具从拾荒零件中创建新设计的人将能够保存电子学。

It does not advocate for going back in time, despite advocating for a dramatic decrease in use of artificial energy, but trusts in human ingenuity to turn problems into solutions, competition into co-operation and waste into resources.

它不主张回到过去，尽管主张大幅减少人造能源的使用，但相信人类的创造力能够将问题转化为解决方案，将竞争转化为合作，将废物转化为资源。

Design for Encapsulation: In order to ensure that, for example, a README file will be readable by any user, distribution disks include a simple text-editing program that can display the README file. Though most users already have one on their systems, software vendors should not to assume that this will be the case.

为封装而设计：为确保例如 README 文件可以被任何用户阅读，分发磁盘包含一个简单的文本编辑程序，可以显示 README 文件。尽管大多数用户已经在他们的系统上有一个，软件供应商不应假设情况会如此。

> When a file is compressed using PKZIP, a decompression program, such as PKUNZIP, is required to expand the file. However, an option in PKZIP allows a simple version of an unzip program to be bundled with each compressed file. Choosing this option creates an executable file which, when run, expands automatically to the original file, avoiding the issue of whether the recipient of a compressed file will have the appropriate decompression software on hand.
> 当文件使用 PKZIP 压缩时，需要解压程序（如 PKUNZIP）来展开文件。然而，PKZIP 中的一个选项允许将简单版本的解压程序与每个压缩文件捆绑在一起。选择此选项会创建一个可执行文件，运行时自动展开为原始文件，避免了压缩文件的接收者是否手边有适当解压软件的问题。

---

| Technique | Description | Pros | Cons |
|-----------|-------------|------|------|
| Migration | Periodically convert data to the next-generation formats | Data is instantly accessible | Copies degrade from generation to generation |
| Emulation | Mimicking the behavior of older hardware with software, tricking old programs into thinking they are running on their original platforms | Data does not need to be altered | Mimicking is seldom perfect; chains of emulators eventually break down |
| Encapsulation | Encase digital data in physical and software wrappers, showing future users how to reconstruct them | Details of interpreting data are never separated from the data themselves | Must build new wrappers for every new format and software release; works poorly for nontextual data |
| Universal virtual computer | Archive paper copies of specifications for a simple, software-defined decoding machine; save all data in a format readable by the machine | Paper lasts for centuries; machine is not tied to specific hardware or software | Difficult to distill specifications into a brief paper document |

| 技术 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| 迁移 | 定期将数据转换为下一代格式 | 数据立即可用 | 从一代到一代复制会退化 |
| 仿真 | 用软件模拟旧硬件的行为，欺骗旧程序使其认为正在其原始平台上运行 | 数据无需更改 | 模拟很少是完美的；仿真器链最终会崩溃 |
| 封装 | 将数字数据封装在物理和软件包装器中，向未来用户展示如何重建它们 | 解释数据的细节永远不会与数据本身分离 | 必须为每种新格式和软件版本构建新的包装器；对非文本数据效果不佳 |
| 通用虚拟计算机 | 存档简单软件定义解码机器的规范的纸质副本；以机器可读的格式保存所有数据 | 纸张可持续数百年；机器不绑定于特定硬件或软件 | 难以将规范提炼成简短的纸质文件 |

---

Any sufficiently organized Piracy is indistinguishable from Preservation.

任何充分组织化的盗版都与保存无法区分。

Emulation is a way of preserving the functionality and access to digital information which might otherwise be lost due to technological obsolescence. One of the benefits of the emulation strategy compared with migration is that the original data need not be altered in any way. It is the emulation of the computer environment that will change with time.

仿真是保存功能和访问数字信息的一种方式，否则这些信息可能因技术过时而丢失。与迁移相比，仿真策略的好处之一是原始数据无需以任何方式更改。会随时间变化的是计算机环境的仿真。

A universal virtual computer needs to be well documented, contain a bare minimum of functionality, be easily testable for compliance, and have very, very few special cases in the specification, since special cases are opportunities for incompatibility; but despite that, it needs to be a reasonable target to write a compiler for. Finally, I argue that a UVC ought to have predictable performance.

通用虚拟计算机需要被良好记录，包含最基本的功能，易于测试合规性，并且规范中非常非常少的特殊情况，因为特殊情况是不兼容的机会；但尽管如此，它需要是一个合理的编译器编写目标。最后，我认为 UVC 应该具有可预测的性能。

Creating may be something we are only able to do, not something we can describe or understand. For this reason, technology must always play a secondary role to the creation we wish to embody in our preserved records and our preserved documents. It would be better to lose everything we have recorded, and all of our technology, than to suffer even the slightest diminution in our ability to create.

创造可能是我们只能做而不能描述或理解的事情。因此，技术必须始终在我们希望在保存的记录和保存的文档中体现的创造中扮演次要角色。失去我们记录的一切和所有技术，也比遭受我们创造能力哪怕最轻微的削弱要好。

Wait. What nostalgia? This is not about reliving fond memories or fetishize about an imaginary past, it's about being tactical in our choice of medium, so as to propagate a political perspective efficiently.

等等。什么怀旧？这不是关于重温美好记忆或迷恋一个想象中的过去，而是关于在我们的媒介选择中保持战术性，以便有效地传播一种政治视角。

---

- [What is scavenge-friendly](https://collapseos.org/why.html)
- [什么是可拾荒的](https://collapseos.org/why.html)

- [Digital-Preservation Proposals](https://www.technologyreview.com/2002/10/01/234717/data-extinction/)
- [数字保存提案](https://www.technologyreview.com/2002/10/01/234717/data-extinction/)

- [PADI's Notes on Emulation](https://web.archive.org/web/20010625015518/http://www.nla.gov.au/padi/topics/19.html)
- [PADI 关于仿真的笔记](https://web.archive.org/web/20010625015518/http://www.nla.gov.au/padi/topics/19.html)

- [Archival with a universal virtual computer](https://dercuano.github.io/notes/uvc-archiving.html)
- [使用通用虚拟计算机存档](https://dercuano.github.io/notes/uvc-archiving.html)

---

## Taking advantage of today's abundance in computing power to prepare for a future in which infrastructures have collapsed.
## 利用今天计算能力的充裕，为未来基础设施崩溃做准备。

Collapse computing prioritizes community needs and aims to contribute to a knowledge commons in order to sustain the practice of computation through infrastructure collapse, it is the practice of engaging with the discarded with an eye to transform what is exhausted and wasted into renewed resources.

崩溃计算优先考虑社区需求，旨在为知识共享做出贡献，以通过基础设施崩溃维持计算实践，它是一种以将被丢弃的东西转化为更新资源的眼光来接触被丢弃事物的实践。

### Failure Scenarios / 失败场景

- **Supply**: Difficulty to get new devices and peripherals because new ones are no longer produced, or unavailable.
- **供应**：由于新产品不再生产或无法获得，难以获得新设备和外设。

- **Power**: Access to power is intermittent, or routed to address more critical needs than powering computers.
- **电力**：电力供应间歇性，或被路由以满足比为计算机供电更关键的需求。

- **Connectivity**: Access to the internet is intermittent due to degraded infrastructure, or prohibited for geopolitical reasons.
- **连接性**：由于基础设施退化或地缘政治原因禁止，互联网访问是间歇性的。

- **Obsolescence**: Inoperability due to undocumented or incompatible peripherals and software.
- **过时**：由于文档缺失或不兼容的外设和软件导致无法运行。

- **Planned Obsolescence**: Inoperability from expiring certificates, copy protection, or other artificial means.
- **计划性淘汰**：由于过期证书、复制保护或其他人为手段导致无法运行。

> Scene from On the Silver Globe (1988)
> 电影《银色星球》(1988) 中的场景

---

Designing for Descent ensures that a system is resilient to intermittent energy supply and network connectivity. Nothing new needs producing and no e-waste needs processing. If your new software no longer runs on old hardware, it is worse than the old software. Software should function on existing hardware and rely on modularity in order to enable a diversity of combinations and implementations. It is about reinventing essential tools so that they are accessible, scalable, sturdy, modular, easy to repair and well documented.

为下降而设计确保系统对间歇性能源供应和网络连接具有弹性。无需生产新产品，也无需处理电子垃圾。如果你的新软件不再在旧硬件上运行，它比旧软件更差。软件应在现有硬件上运行，并依赖模块化以启用多样化的组合和实现。它是关于重新发明基本工具，使它们可访问、可扩展、坚固、模块化、易于维修且有良好文档。

> Kelvin versioning uses integers in degrees Kelvin, counting down toward a final specification, upon reaching absolute zero, it is frozen. Further updates are no longer possible. It contrasts with how typical software is designed to indefinitely increase in scope, and complexity.
> 开尔文版本控制使用以开尔文度为单位的整数，倒数至最终规范，达到绝对零度时即被冻结。不再可能有进一步更新。这与典型软件被设计为无限增加范围和复杂性形成对比。

A post-collapse society that has eventually lost all of its artificial computing capacity may still want to continue the practice of computer science for various reasons.

一个最终失去所有人工计算能力的崩溃后社会可能仍希望出于各种原因继续计算机科学实践。

---

> Four Concepts Of Resilience
> 韧性的四个概念

**Agility** / **敏捷性**

The capacity to adapt or respond rapidly to a changing environment.

适应或快速响应变化环境的能力。

**Preparedness** / **准备性**

The ability to reflect on past threats, and bouncing forward by enacting new ideas for development after a crisis event.

反思过去威胁的能力，通过在危机事件后实施新的发展想法来向前反弹。

**Elasticity** / **弹性**

Increasing the exchangeability and flexibility of relationships among people and things within an organization and a wider ecosystem.

增加组织及其更广泛生态系统内人员和事物之间关系的可交换性和灵活性。

**Redundancy** / **冗余**

The intentional duplication of critical components with the goal of increasing the reliability of a system.

故意复制关键组件以增加系统可靠性的目标。

---

> Even Check
> 偶数校验

| Message | Parity Bit |
|---------|-----------|
| 1101 0011 | 0 |

| 消息 | 奇偶校验位 |
|------|-----------|
| 1101 0011 | 0 |

Designing for decay is to harden messages via error correction by transmitting additional information to catch information loss or tampering. For example, by adding a parity bit that corresponds to the odd or even number of active bits in a specific length of data.

为衰减而设计是通过传输额外信息来捕获信息丢失或篡改，从而通过纠错来硬化消息。例如，通过添加对应于特定长度数据中活动位奇偶性的奇偶校验位。

The Arecibo Message is an interstellar radio message for which receiver's capabilities are unknown, the length of the message was chosen to be a semiprime so its dimension(73 rows by 23 columns) could be inferred from an otherwise totally headerless message.

阿雷西博信息是一条星际无线电信息，由于接收者的能力是未知的，消息的长度被选择为半素数，因此其维度（73行×23列）可以从原本完全无头的消息中推断出来。

> A decay hardened quine refers to a programming concept where a quine program, a program that prints its own source code, is designed to be robust against character corruption or "decay."
> 衰减硬化自产生程序指的是一种编程概念，其中自产生程序（打印自身源代码的程序）被设计为对字符损坏或"衰减"具有鲁棒性。

And one voice, with sublime disregard for the situation, read poetry aloud in the firey study, until all the film spools burned, until all the wires withered and the circuits cracked.

一个声音，以崇高的无视情境，在火红的书房中大声朗读诗歌，直到所有胶片卷轴燃尽，直到所有电线枯萎，电路断裂。

> — Ray Bradbury, There Will Come Soft Rains
> — 雷·布拉德伯里，《细雨将至》

---

- [CollapseOS](http://collapseos.org)
- [崩溃操作系统](http://collapseos.org)

- [Where did that prebuilt binary come from?](https://bootstrapping.miraheze.org/wiki/Main_Page)
- [那个预构建二进制文件从何而来？](https://bootstrapping.miraheze.org/wiki/Main_Page)

- [Four concepts for resilience](https://www.researchgate.net/publication/276139783_Four_concepts_for_resilience_and_the_implications_for_the_future_of_resilience_engineering)
- [韧性的四个概念](https://www.researchgate.net/publication/276139783_Four_concepts_for_resilience_and_the_implications_for_the_future_of_resilience_engineering)

- [Terminal Event Management Policy](https://en.wikipedia.org/wiki/Wikipedia:Terminal_Event_Management_Policy), satire
- [终端事件管理政策](https://en.wikipedia.org/wiki/Wikipedia:Terminal_Event_Management_Policy)，讽刺

---

## 批判性思考与评论 / Critical Thinking Commentary

### 作者的核心论点分析

本文提出了一套完整的"永久计算"（Permacomputing）哲学框架，其核心论点可以归纳为以下几个方面：

**1. 资源有限性前提**
作者明确将计算资源视为有限资源，这与当前主流技术产业将计算能力视为无限可扩展的默认假设形成鲜明对比。这种前提转换是根本性的——它要求我们从"如何更快地使用资源"转变为"如何更久地使用资源"。

**2. 从自然生态借智慧**
文章标题明确提到了"朴门永续设计"（permaculture）的启发。这是一种从农业生态学借鉴的方法论：观察自然系统的自我维持机制，并将其应用于人工系统设计。这种跨学科类比非常有价值——它提醒我们计算系统也是一种生态系统。

**3. 分层应对策略**
作者提出了三个递进层次的计算策略：节俭计算（Frugal Computing）→ 抢救计算（Salvage Computing）→ 崩溃计算（Collapse Computing）。这种分层不是简单的程度差异，而是代表了不同崩溃程度下的生存策略，体现了深刻的韧性思维。

### 优点与亮点

**优点一：系统性与可操作性兼备**
文章不仅提出了哲学理念，还给出了具体的设计原则（可拆卸设计、可延展系统、可逆性设计、差异兼容设计等）。这些原则不是抽象口号，而是可以实际指导工程决策的规范。

**优点二：技术与人文的平衡**
作者深入讨论了 Forth 语言、UVC（通用虚拟计算机）、奇偶校验位等技术细节，同时不忘讨论"自淘汰系统"这样的社会哲学概念。这种平衡在技术写作中非常难得。

**优点三：对主流技术产业的批判**
"无法维修的东西已经坏了"——这句话直接挑战了当代消费电子产业的计划性淘汰策略。文章明确指出"如果你的新软件不再在旧硬件上运行，它比旧软件更差"，这是对软件工程领域的当头棒喝。

**优点四：开尔文版本控制的创新**
用开尔文温度倒数来度量软件成熟度，达到"绝对零度"即冻结，这种类比既诗意又精确。它挑战了软件必须不断更新的迷思。

### 弱点与局限

**弱点一：理想主义的实现困境**
虽然文章提出了许多美好的设计原则，但在现实商业环境中实施这些原则面临巨大阻力。企业追求利润最大化，而永久计算追求的是资源使用最小化，这两者存在根本性张力。文章对如何克服这种结构性矛盾着墨甚少。

**弱点二：对"崩溃"的模糊性**
文章频繁提及"基础设施崩溃"（infrastructure collapse），但并未明确定义这是什么程度的崩溃。是电网瘫痪？互联网中断？还是文明级别的灾难？不同情境下的技术需求差异巨大，而文章的解决方案似乎试图同时应对所有情况。

**弱点三：技术精英主义的隐忧**
"那些能够利用低技术工具从拾荒零件中创建新设计的人将能够保存电子学"——这句话暗示了一种技术精英主义的筛选机制。在真正的崩溃情境中，能够掌握这些技能的人群可能非常有限，这是否会加剧社会不平等？

**弱点四：对现有经济体系的依赖**
抢救计算依赖于垃圾填埋场中"数十亿"的废弃电子零件。但这些零件之所以存在，正是因为现行经济体系持续生产它们。如果崩溃真的发生，这些库存终将耗尽，届时该怎么办？

### 个人批判视角

**关于"复古计算"的辨析**
文章试图区分永久计算与"怀旧"（nostalgia），声称这不是关于"重温美好记忆或迷恋想象中的过去"。然而，从实践层面看，许多永久计算倡导者确实表现出对旧技术的浪漫化倾向。例如，对 Forth 语言、老式8位计算机的推崇，在某种程度上确实带有怀旧色彩。

这不是批评本身，但需要诚实面对：技术怀旧与技术韧性之间的界限往往是模糊的。承认这一点反而能增强论点的可信度——我们不必假装这些选择完全是理性的；情感连接也是可持续使用的重要动力。

**关于"自淘汰系统"的矛盾**
文章批评了"最大化用户参与度的系统"，提倡"自淘汰系统"。但这里存在一个内在张力：一个成功的自淘汰系统，其最终结果是自己的消亡。那么谁来维护这个系统的代码？谁来确保它在"淘汰自己"之前完成了使命？

或许更现实的路径是"渐进式自简化"——系统不会完全消失，而是逐渐将功能迁移到更简单、更本地的解决方案上。

**关于"可延展性"的代价**
文章对"可延展系统"（malleable systems）的赞美，忽视了软件工程中一个基本真理：灵活性是有代价的。高度可延展的系统往往更难理解、更难调试、更难保证安全性。

"深度可延展性"（Deep Malleability）听起来美好，但在实践中可能意味着"任何人都可以修改任何东西"——这既是祝福也是诅咒。文章没有充分讨论如何平衡可延展性与可靠性。

### 深层影响与启示

**对软件行业的启示**
永久计算提出了一个根本性问题：我们是否在构建值得被保存的软件？如果答案是"不"，那么整个行业的价值基础都值得重新审视。这可能催生一种新的软件质量评估标准——不是功能多寡，而是"可存续性"（survivability）。

**对教育的启示**
如果永久计算的愿景部分成真，那么计算机教育需要重大转向。目前的编程教育侧重于最新框架和工具，而永久计算强调的是底层原理、硬件理解、逆向工程和维修技能。这可能意味着"复古计算"应该成为计算机科学教育的一部分。

**对政策制定的启示**
文章隐含的"维修权"（Right to Repair）主张具有强烈的政策含义。欧盟已经开始在这方面立法，要求产品必须可维修。永久计算为这些政策提供了理论支撑。

**对后增长经济的预演**
永久计算本质上是一种"后增长"（post-growth）的技术观——它假设经济增长不是无限的，技术迭代也不是越快越好。在一个资源约束日益收紧的世界，这种思维方式可能从边缘走向主流。

### 结语

永久计算是一个既激进又保守的技术哲学。它激进地挑战了当代技术产业的基本假设，却又保守地强调维护、维修和延长使用周期。这种张力正是其价值所在——它迫使我们思考：技术进步的真正含义是什么？

文章结尾引用了雷·布拉德伯里的《细雨将至》——一个后末日世界中，自动房屋在主人已死、世界已毁后仍机械地运转的荒诞场景。这个引用意味深长：技术可以在没有人类的情况下继续运行，但如果我们的技术不能在我们最需要它的时候（比如崩溃之后）继续为人类服务，那么它的"智能"又有何意义？

永久计算不是一个答案，而是一组问题。它邀请我们重新思考技术与人类、与自然、与未来的关系。在这个意义上，这篇文章的价值不在于它提供了什么具体解决方案，而在于它成功地破坏了我们对"进步"的默认理解。

---

*incoming: [devlog](devlog.html) [now lie in it](now_lie_in_it.html) [2026](2026.html) [2024](2024.html) [2023](2023.html) [2022](2022.html) [solarpunk](solarpunk.html) [hundred rabbits](hundred_rabbits.html)*
